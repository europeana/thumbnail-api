apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: translation-tmp-ingress
  annotations:
    ${K8S_INGRESS_ANNOTATIONS}
spec:
  ingressClassName: public-iks-k8s-nginx
  tls:
    - hosts:
        - ${K8S_HOSTNAME1}
      secretName: ${K8S_SECRETNAME}
  rules:
    - host: ${K8S_HOSTNAME1}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: translation-tmp-service
                port:
                  number: 80

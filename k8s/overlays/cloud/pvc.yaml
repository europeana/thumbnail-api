apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-thumbnail-test
spec:
  storageClassName: ibmc-vpc-block-general-purpose
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
